# TINK (Trading Insight Neural Kernel) 개발 히스토리

**작성 일시:** 2026년 8월 31일 07:14
**프로젝트 경로:** `c:\Users\USER\Documents\07. AntiGravity\13. CurrencyDashboard`

---

## 1. 프로젝트 리브랜딩 및 기본 구조 변경
* **타이틀 변경:** 프로젝트의 성격과 철학을 담아 대시보드 이름을 **TINK (Trading Insight Neural Kernel)**로 명명.
* **UI 정리:** 실시간 자동 업데이트(1시간 주기) 아키텍처에 맞지 않는 불필요한 "Get Recent Data" 버튼 삭제.

## 2. 신흥국 및 주요 교역국 통화 추가
* **대상 통화:** 베트남(VND), 대만(TWD), 호주(AUD) 추가
* **이슈 해결 (VND):** 야후 파이낸스에서 VND/KRW 직거래 환율 데이터를 제공하지 않는 문제 발생. USD/KRW를 USD/VND로 나누는 **교차 환율(Cross Rate) 로직**을 파이썬 백엔드에 직접 구현하여 해결.
* **정밀도 향상:** 소수점이 매우 작은 통화(예: VND 등 환율이 10 미만인 경우) 차트가 일직선으로 보이는 현상을 수정하기 위해, 10 미만의 값은 소수점 4자리까지 표시하도록 파이썬 및 자바스크립트 로직 수정.

## 3. 다중 타임프레임(Multi-Timeframe) 분석 기능 완벽 구현
* **파이썬 백엔드 (데이터 일괄 호출):**
  * 야후 파이낸스에서 한 번 호출 시 과거 1년 치가 아닌 **과거 5년 치(약 1,250영업일) 데이터**를 통째로 가져오도록 변경 (`period="5y"`).
  * **중요 철학 유지:** 5년 치 데이터를 불러오더라도, 각 인덱스(V1, V2, V3)의 기준점(100)을 정하는 분모는 절대 변하지 않도록 무조건 **'가장 최근 252일(1년)의 평균'**으로 강력히 고정.
* **자바스크립트 프론트엔드 (동적 렌더링):**
  * 메인 UI 우측 상단에 3개월, 6개월, 1년, 3년, 5년 뷰를 선택할 수 있는 **드랍다운 메뉴** 신설.
  * 서버에서 한 번 받아온 5년 치 데이터를 자바스크립트가 `sliceData()` 함수를 통해 즉석에서 잘라서(Slice) 차트를 다시 그리도록 아키텍처 개편.
  * 기간 변경 시 **최고/최저/평균/중앙값 통계가 선택된 기간에 맞추어 실시간 재계산**되도록 로직 구현.
  * 하단 텍스트 라벨("52주 평균" 등)도 선택한 기간("3개월 평균", "5년 평균" 등)에 맞게 동적으로 변경되도록 수정.

## 4. 고급 퀀트 기능: 동적 재정규화 (Dynamic Re-normalization)
* **요구 사항:** 사용자가 '3개월'을 선택하면, 과거 1년이 아니라 딱 '과거 3개월의 평균'을 100으로 리셋해서 단기 추세의 강약을 보고 싶다는 요청.
* **구현 내역:** 
  * 자바스크립트 내에 `isCustomIndex` 플래그를 신설.
  * 사용자가 기간을 변경할 때마다, 일반 환율(USD/KRW)은 기존 숫자를 유지하되, **인덱스 차트(V1, V2, V3)는 해당 타임프레임의 평균을 100으로 맞추도록 데이터를 동적 스케일링(Scaling)** 처리.
  * 이를 통해 단기 6개월 평균 기준 최악의 바닥을 찍고 반등 중인 현재 원화의 강한 회복세를 105점 등의 상대 점수로 뚜렷하게 식별할 수 있게 됨.

## 5. 트러블슈팅 및 버그 픽스
* **JS 문법 오류:** 템플릿 리터럴(`$`, 백틱) 이스케이프 문제로 발생했던 `app.js` 문법 에러 수정 및 복구.
* **UI 가독성 이슈:** 다크 모드에서 드랍다운 메뉴창의 배경이 흰색으로 떠서 글자가 안 보이던 문제 해결 (`background-color: var(--bg-main)` 적용).
* **캐시 꼬임 현상:** 자바스크립트 로직이 업데이트되었음에도 브라우저가 과거 코드를 기억하고 있던 문제(Stat 미반영 현상)를 해결하기 위해 `index.html` 내 스크립트 호출부에 Cache Busting(`app.js?v=3`) 적용.
* **인덱스 필터링 버그:** 자바스크립트에서 인덱스를 판별할 때 `startsWith('V1')` 로직이 한글 이름("달러 인덱스...") 때문에 실패하던 것을 `isCustomIndex` 불리언 값 주입 방식으로 해결.

## 6. 트레이딩 이론 및 인사이트 논의 요약
1. **야후 API 5년 데이터와 실시간 환율의 관계:** 일봉 데이터를 부르더라도 맨 마지막 데이터는 장중 실시간 현재가가 포함됨을 확인. (1시간 단위 갱신 아키텍처의 정당성 확보)
2. **캐싱 vs Stateless 아키텍처:** 데이터를 로컬(DB/CSV)에 저장하기보다는 매번 새로 부르는(Stateless) 방식이 유지보수와 에러 복구, 수정 주가 반영에 훨씬 유리함을 논의 및 채택.
3. **분할 매매 기법 논의:** 환율은 주식(우상향)과 달리 **평균 회귀(Mean Reverting)** 성질을 가지므로, 시간 기준(DCA) 매수보다는 **가격(지표) 기준 분할 매매(그리드 매매)**가 압도적으로 유리함을 확인. TINK 대시보드의 V3 인덱스 점수를 활용한 박스권 스윙 매매가 최고의 조합임을 결론.
